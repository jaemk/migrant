
# Required, do not edit
database_type = "sqlite"

# Required: Absolute or relative path to your database file.
#           If a relative path is provided, it will be resolved
#           relative to the directory containing this config file.
# ex.) database_name = "db/db.db"
database_path = "db/db.db"

migration_location = "migrations/managed"  # default "migrations"

